services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: buildvision3d
      POSTGRES_USER: buildvision3d
      POSTGRES_PASSWORD: buildvision3d
    ports:
      - "5432:5432"
    volumes:
      - controller_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U buildvision3d -d buildvision3d"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      dockerfile: Dockerfile.controller
    command: uvicorn controller_api.main:app --host 0.0.0.0 --port 8000
    working_dir: /app
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://buildvision3d:buildvision3d@postgres:5432/buildvision3d
      CONTROLLER_ID: ${CONTROLLER_ID:-compose-api}
      CONTROLLER_POLL_INTERVAL_SECONDS: ${CONTROLLER_POLL_INTERVAL_SECONDS:-2}
      CONTROLLER_STAGE_PYTHON_BIN: python
      FAKE_STAGE_DURATION_SECONDS: ${FAKE_STAGE_DURATION_SECONDS:-1}
      CONTROLLER_DEFAULT_COLMAP_PROVIDER: ${CONTROLLER_DEFAULT_COLMAP_PROVIDER:-local_fake}
      CONTROLLER_DEFAULT_TRAINING_PROVIDER: ${CONTROLLER_DEFAULT_TRAINING_PROVIDER:-runpod_training}
      CONTROLLER_REPO_URL: ${CONTROLLER_REPO_URL:-}
      CONTROLLER_GIT_REF: ${CONTROLLER_GIT_REF:-main}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION:-auto}
      R2_ENDPOINT: ${R2_ENDPOINT:-}
      R2_BUCKET: ${R2_BUCKET:-buildvision3d-pipeline}
      RUNPOD_COLMAP_IMAGE: ${RUNPOD_COLMAP_IMAGE:-docker.io/blackjokuro/buildvision3d-colmap-gpu:cuda12.4-colmap-r2-runtime-onnx-cudnn-pycolmap-sm75-sm86-sm89-r1}
      RUNPOD_COLMAP_GPU_TYPES: ${RUNPOD_COLMAP_GPU_TYPES:-NVIDIA RTX A6000,NVIDIA RTX 4090,NVIDIA L40S,NVIDIA L4}
      RUNPOD_COLMAP_CLOUD_TYPE: ${RUNPOD_COLMAP_CLOUD_TYPE:-COMMUNITY}
      RUNPOD_COLMAP_CONTAINER_DISK_GB: ${RUNPOD_COLMAP_CONTAINER_DISK_GB:-20}
      RUNPOD_COLMAP_POLL_SECONDS: ${RUNPOD_COLMAP_POLL_SECONDS:-30}
      RUNPOD_COLMAP_TIMEOUT_SECONDS: ${RUNPOD_COLMAP_TIMEOUT_SECONDS:-7200}
      RUNPOD_TRAINING_IMAGE: ${RUNPOD_TRAINING_IMAGE:-docker.io/blackjokuro/buildvision3d-nerfstudio-splatfacto-gpu:cuda11.8-pixi-splatfacto-r2-optbuildvision-sm75-sm86-sm89-r3}
      RUNPOD_TRAINING_GPU_TYPES: ${RUNPOD_TRAINING_GPU_TYPES:-NVIDIA RTX A4500}
      RUNPOD_TRAINING_CLOUD_TYPE: ${RUNPOD_TRAINING_CLOUD_TYPE:-SECURE}
      RUNPOD_TRAINING_CONTAINER_DISK_GB: ${RUNPOD_TRAINING_CONTAINER_DISK_GB:-20}
      RUNPOD_TRAINING_POLL_SECONDS: ${RUNPOD_TRAINING_POLL_SECONDS:-30}
      RUNPOD_TRAINING_TIMEOUT_SECONDS: ${RUNPOD_TRAINING_TIMEOUT_SECONDS:-14400}
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health').read()"]
      interval: 10s
      timeout: 5s
      retries: 10

  worker:
    build:
      context: .
      dockerfile: Dockerfile.controller
    command: python -m controller_worker
    working_dir: /app
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://buildvision3d:buildvision3d@postgres:5432/buildvision3d
      CONTROLLER_ID: ${CONTROLLER_ID:-compose-worker}
      CONTROLLER_POLL_INTERVAL_SECONDS: ${CONTROLLER_POLL_INTERVAL_SECONDS:-2}
      CONTROLLER_STAGE_PYTHON_BIN: python
      FAKE_STAGE_DURATION_SECONDS: ${FAKE_STAGE_DURATION_SECONDS:-1}
      CONTROLLER_DEFAULT_COLMAP_PROVIDER: ${CONTROLLER_DEFAULT_COLMAP_PROVIDER:-local_fake}
      CONTROLLER_DEFAULT_TRAINING_PROVIDER: ${CONTROLLER_DEFAULT_TRAINING_PROVIDER:-runpod_training}
      CONTROLLER_REPO_URL: ${CONTROLLER_REPO_URL:-}
      CONTROLLER_GIT_REF: ${CONTROLLER_GIT_REF:-main}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION:-auto}
      R2_ENDPOINT: ${R2_ENDPOINT:-}
      R2_BUCKET: ${R2_BUCKET:-buildvision3d-pipeline}
      RUNPOD_COLMAP_IMAGE: ${RUNPOD_COLMAP_IMAGE:-docker.io/blackjokuro/buildvision3d-colmap-gpu:cuda12.4-colmap-r2-runtime-onnx-cudnn-pycolmap-sm75-sm86-sm89-r1}
      RUNPOD_COLMAP_GPU_TYPES: ${RUNPOD_COLMAP_GPU_TYPES:-NVIDIA RTX A6000,NVIDIA RTX 4090,NVIDIA L40S,NVIDIA L4}
      RUNPOD_COLMAP_CLOUD_TYPE: ${RUNPOD_COLMAP_CLOUD_TYPE:-COMMUNITY}
      RUNPOD_COLMAP_CONTAINER_DISK_GB: ${RUNPOD_COLMAP_CONTAINER_DISK_GB:-20}
      RUNPOD_COLMAP_POLL_SECONDS: ${RUNPOD_COLMAP_POLL_SECONDS:-30}
      RUNPOD_COLMAP_TIMEOUT_SECONDS: ${RUNPOD_COLMAP_TIMEOUT_SECONDS:-7200}
      RUNPOD_TRAINING_IMAGE: ${RUNPOD_TRAINING_IMAGE:-docker.io/blackjokuro/buildvision3d-nerfstudio-splatfacto-gpu:cuda11.8-pixi-splatfacto-r2-optbuildvision-sm75-sm86-sm89-r3}
      RUNPOD_TRAINING_GPU_TYPES: ${RUNPOD_TRAINING_GPU_TYPES:-NVIDIA RTX A4500}
      RUNPOD_TRAINING_CLOUD_TYPE: ${RUNPOD_TRAINING_CLOUD_TYPE:-SECURE}
      RUNPOD_TRAINING_CONTAINER_DISK_GB: ${RUNPOD_TRAINING_CONTAINER_DISK_GB:-20}
      RUNPOD_TRAINING_POLL_SECONDS: ${RUNPOD_TRAINING_POLL_SECONDS:-30}
      RUNPOD_TRAINING_TIMEOUT_SECONDS: ${RUNPOD_TRAINING_TIMEOUT_SECONDS:-14400}
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  controller_postgres_data:
